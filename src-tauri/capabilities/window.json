{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "window",
  "description": "Window domain permissions for native window lifecycle and titlebar interactions. Commands: apply_window_stage / complete_startup_transition / set_window_background.",
  "windows": [
    "main",
    "welcome"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
