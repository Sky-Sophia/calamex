# router/index.ts — 当前状态：已启用（Active）

> **@status: active**

## 当前用途

本项目当前仅保留受限路由能力，用于主窗口工作台入口：

- `/home`：工作台路由锚点
- `/` 与未知路径：重定向到 `/home`

## 边界约束

- 路由 **不是** 工作台业务编排真源
- `App.vue` 仅负责全局宿主与错误覆盖层
- 新增业务路由前，必须继续遵守当前受限范围与评审要求
